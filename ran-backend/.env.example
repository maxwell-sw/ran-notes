# 复制为 .env 后填入真实值。改模型只需改 OPENAI_MODEL 并重启后端。

OPENAI_API_KEY=你的API密钥
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=deepseek/deepseek-v4-flash-vision-exp
# 设置为 false 时仅发送提取出的文本；多模态模型建议保留 true。
VISION_INPUT_ENABLED=true
VISION_MAX_IMAGES=12
